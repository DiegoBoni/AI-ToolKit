using UnityEngine;

/// <summary>
/// Use this attribute on a string field to make it display as a password field in the Inspector.
/// </summary>
public class PasswordAttribute : PropertyAttribute { }
